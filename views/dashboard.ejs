<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titulo %></title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Animate.css -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/assets/css/dashboard.css">
    <script type="module" src="/assets/js/main.js"></script>
</head>
<body data-page="dashboard">
    <!-- Layout Container -->
    <div class="d-flex">
        <!-- Sidebar -->
        <nav class="sidebar">
            <!-- Brand -->
            <div class="brand">
                <div class="d-flex align-items-center justify-content-center py-4">
                    <a href="/" style="display: flex; align-items: center; justify-content: center; text-decoration: none;">
                        <img src="/assets/images/logo.jpg" alt="PapaIA Logo" style="height: 80px; width: auto; border-radius: 12px; box-shadow: 0 4px 15px rgba(0,0,0,0.2); transition: all 0.3s ease;" class="logo-link">
                    </a>
                </div>
            </div>
            
            <!-- Navigation -->
            <ul class="nav flex-column py-3">
                <li class="nav-item">
                    <a href="/dashboard" class="nav-link active">
                        <i class="fas fa-home"></i>
                        Dashboard Principal
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/clasificacion" class="nav-link">
                        <i class="fas fa-camera"></i>
                        Clasificar Papa 📷
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/reportes" class="nav-link">
                        <i class="fas fa-chart-line"></i>
                        Reportes y Analytics
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/historial" class="nav-link">
                        <i class="fas fa-history"></i>
                        Historial de Análisis
                    </a>
                </li>
                
                <% if (usuario.rol === 'administrador') { %>
                <li class="nav-item mt-3">
                    <small class="text-white-50 px-3 text-uppercase fw-bold">Administración</small>
                </li>
                <li class="nav-item">
                    <a href="/gestion-usuarios" class="nav-link">
                        <i class="fas fa-users-cog"></i>
                        Gestión de Usuarios
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/admin/modelo" class="nav-link">
                        <i class="fas fa-brain"></i>
                        Estado del Modelo IA
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/admin/configuracion" class="nav-link">
                        <i class="fas fa-cogs"></i>
                        Configuración Sistema
                    </a>
                </li>
                <% } %>
                
                <li class="nav-item mt-4">
                    <a href="/logout" class="nav-link">
                        <i class="fas fa-sign-out-alt"></i>
                        Cerrar Sesión
                    </a>
                </li>
            </ul>
        </nav>
        
        <!-- Main Content -->
        <main class="main-content">
            <div class="content-wrapper">
                <!-- Header -->
                <div class="dashboard-header animate__animated animate__fadeIn">
                    <div class="row align-items-center">
                        <div class="col-lg-8">
                            <div class="d-flex align-items-center">
                                <div class="user-avatar me-3">
                                    <% if (usuario.avatarUrl) { %>
                                        <img src="<%= usuario.avatarUrl %>" alt="Avatar" style="width:100%;height:100%;object-fit:cover;border-radius:50%;">
                                    <% } else { %>
                                        <%= usuario.nombre.charAt(0).toUpperCase() %>
                                    <% } %>
                                </div>
                                <div>
                                    <h2 class="mb-1">¡Bienvenido de vuelta, <%= usuario.nombre %>! 🌱</h2>
                                    <p class="text-muted mb-0">
                                        Rol: <span class="badge bg-success"><%= usuario.rol.charAt(0).toUpperCase() + usuario.rol.slice(1) %></span>
                                        • Último acceso: <%= fechaUltimoAcceso ? new Date(fechaUltimoAcceso).toLocaleDateString('es-PE') : 'Primera vez' %>
                                    </p>
                                </div>
                            </div>
                        </div>
                        <div class="col-lg-4 text-end">
                            <button type="button" class="btn btn-outline-success me-2" onclick="location.reload()">
                                <i class="fas fa-sync-alt me-1"></i>
                                Actualizar
                            </button>
                            <div class="dropdown d-inline-block">
                                <button class="btn btn-success dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    <% if (usuario.fotoPerfil) { %>
                                        <img src="<%= usuario.fotoPerfil %>" class="rounded-circle me-2" width="24" height="24" alt="Perfil">
                                    <% } else { %>
                                        <i class="fas fa-user me-1"></i>
                                    <% } %>
                                    <%= usuario.nombre %>
                                </button>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    <li>
                                        <a class="dropdown-item" href="/perfil">
                                            <i class="fas fa-user-circle me-2"></i>Mi Perfil
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" href="/historial">
                                            <i class="fas fa-history me-2"></i>Mis Clasificaciones
                                        </a>
                                    </li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                        <a class="dropdown-item text-danger" href="/logout">
                                            <i class="fas fa-sign-out-alt me-2"></i>Cerrar Sesión
                                        </a>
                                    </li>
                                </ul>
                            </div>
                        </div>
                    </div>  
                </div>
                
                <!-- Statistics Cards -->
                <div class="stats-grid animate__animated animate__fadeInUp">
                    <div class="stat-card quality-card">
                        <div class="stat-icon quality">
                            <i class="fas fa-check-circle"></i>
                        </div>
                        <div class="stat-value" id="stat-calidad"><%= stats.clasificacionesHoy %></div>
                        <div class="stat-label">Papas Clasificadas Hoy</div>
                        <div class="stat-change <%= stats.variacionMes > 0 ? 'positive' : 'negative' %>">
                            <i class="fas fa-arrow-<%= stats.variacionMes > 0 ? 'up' : 'down' %> me-1"></i>
                            <%= stats.variacionMes > 0 ? '+' : '' %><%= stats.variacionMes %>% vs mes anterior
                        </div>
                        <div class="progress-eco">
                            <% var progWidth = Math.min((stats.clasificacionesHoy / Math.max(stats.totalClasificaciones / 30, 1)) * 100, 100); %>
                            <div class="progress-bar-eco" style="width: <%= progWidth %>%; "></div>
                        </div>
                    </div>
                    
                    <div class="stat-card analysis-card">
                        <div class="stat-icon analysis">
                            <i class="fas fa-microscope"></i>
                        </div>
                        <div class="stat-value" id="stat-analisis"><%= stats.totalClasificaciones.toLocaleString('es-PE') %></div>
                        <div class="stat-label">Análisis Realizados</div>
                        <div class="stat-change positive">
                            <i class="fas fa-arrow-up me-1"></i>
                            <% if (stats.aptos + stats.noAptos > 0) { %>
                                <%= ((stats.aptos / (stats.aptos + stats.noAptos)) * 100).toFixed(1) %>% aptos
                            <% } else { %>
                                Sin datos
                            <% } %>
                        </div>
                        <div class="progress-eco">
                            <div class="progress-bar-eco" style="width: 85%"></div>
                        </div>
                    </div>
                    
                    <div class="stat-card success-card">
                        <div class="stat-icon success">
                            <i class="fas fa-trophy"></i>
                        </div>
                        <div class="stat-value" id="stat-precision"><%= stats.confianzaPromedio %>%</div>
                        <div class="stat-label">Precisión del Modelo</div>
                        <div class="stat-change positive">
                            <i class="fas fa-arrow-up me-1"></i>
                            Confianza promedio
                        </div>
                        <div class="progress-eco">
                            <div class="progress-bar-eco" style="width: <%= stats.confianzaPromedio %>%; "></div>
                        </div>
                    </div>
                    
                    <div class="stat-card processing-card">
                        <div class="stat-icon processing">
                            <i class="fas fa-clock"></i>
                        </div>
                        <div class="stat-value" id="stat-tiempo"><%= stats.tiempoPromedio %>s</div>
                        <div class="stat-label">Tiempo Promedio</div>
                        <div class="stat-change positive">
                            <i class="fas fa-arrow-down me-1"></i>
                            ms/análisis
                        </div>
                        <div class="progress-eco">
                            <div class="progress-bar-eco" style="width: 65%"></div>
                        </div>
                    </div>
                </div>
                
                <!-- Quick Actions -->
                <div class="quick-actions animate__animated animate__fadeInUp animate__delay-1s">
                    <h3 class="mb-0">🚀 Acciones Rápidas</h3>
                    <p class="text-muted">Herramientas más utilizadas para la clasificación de papa</p>
                    
                    <div class="action-grid">
                        <a href="/clasificacion" class="action-btn">
                            <div class="action-icon">
                                <i class="fas fa-camera"></i>
                            </div>
                            <div>
                                <div class="fw-semibold">Nueva Clasificación</div>
                                <small class="text-muted">Analizar nueva imagen</small>
                            </div>
                        </a>
                        
                        <a href="/clasificacion/lote" class="action-btn">
                            <div class="action-icon">
                                <i class="fas fa-layer-group"></i>
                            </div>
                            <div>
                                <div class="fw-semibold">Análisis por Lotes</div>
                                <small class="text-muted">Múltiples imágenes</small>
                            </div>
                        </a>
                        
                        <a href="/reportes" class="action-btn">
                            <div class="action-icon">
                                <i class="fas fa-chart-pie"></i>
                            </div>
                            <div>
                                <div class="fw-semibold">Ver Reportes</div>
                                <small class="text-muted">Estadísticas detalladas</small>
                            </div>
                        </a>
                        
                        <a href="/exportar" class="action-btn">
                            <div class="action-icon">
                                <i class="fas fa-download"></i>
                            </div>
                            <div>
                                <div class="fw-semibold">Exportar Datos</div>
                                <small class="text-muted">CSV, PDF, Excel</small>
                            </div>
                        </a>
                    </div>
                </div>
                
                <!-- Charts and Recent Activity Row -->
                <div class="row">
                    <!-- Chart -->
                    <div class="col-lg-8">
                        <div class="chart-container animate__animated animate__fadeInLeft animate__delay-2s">
                            <div class="chart-header">
                                <h4 class="mb-0">📈 Clasificaciones por Variedad (Últimos 7 días)</h4>
                                <div class="btn-group btn-group-sm">
                                    <button class="btn btn-outline-success active">7 días</button>
                                    <button class="btn btn-outline-success">30 días</button>
                                    <button class="btn btn-outline-success">90 días</button>
                                </div>
                            </div>
                            <div class="chart-placeholder">
                                <% if (clasificacionesPorVariedad && clasificacionesPorVariedad.length > 0) { %>
                                    <div class="row mt-3">
                                        <% clasificacionesPorVariedad.forEach(function(item) { %>
                                        <div class="col-md-4 mb-3">
                                            <div class="text-center">
                                                <h5><%= item.nombreComun %></h5>
                                                <h3 class="text-success"><%= item.cantidad %></h3>
                                                <div class="progress">
                                                    <div class="progress-bar bg-success" role="progressbar" aria-valuemin="0" aria-valuemax="100" style="width: <%= item.porcentaje %>%; ">
                                                        <%= item.porcentaje.toFixed(1) %>%
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                        <% }); %>
                                    </div>
                                <% } else { %>
                                    <div class="text-center">
                                        <i class="fas fa-chart-area fs-1 mb-3 text-success"></i>
                                        <p>Sin clasificaciones en los últimos 7 días</p>
                                    </div>
                                <% } %>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Recent Activity -->
                    <div class="col-lg-4">
                        <div class="recent-activity animate__animated animate__fadeInRight animate__delay-2s">
                            <h4 class="mb-3">🕒 Actividad Reciente</h4>
                            
                            <% if (actividadReciente && actividadReciente.length > 0) { %>
                                <% actividadReciente.forEach(function(item) { %>
                                <div class="activity-item">
                                    <div class="activity-icon <%= item.condicion === 'apto' ? 'success' : 'warning' %>">
                                        <i class="fas fa-<%= item.condicion === 'apto' ? 'check' : 'times' %>"></i>
                                    </div>
                                    <div class="activity-content">
                                        <div class="activity-title">
                                            <%= item.idVariedad ? item.idVariedad.nombreComun : 'Papa' %> - <%= item.condicion %>
                                        </div>
                                        <div class="activity-description">
                                            Confianza: <%= item.confianzaFormato %> • <%= item.idUsuario ? item.idUsuario.nombre : 'Usuario' %>
                                        </div>
                                    </div>
                                    <div class="activity-time"><%= item.fechaFormato %></div>
                                </div>
                                <% }); %>
                            <% } else { %>
                                <div class="text-center text-muted py-4">
                                    <i class="fas fa-inbox fs-3 mb-2"></i>
                                    <p>No hay actividad reciente</p>
                                </div>
                            <% } %>
                                <div class="activity-time">Hace 2 horas</div>
                            </div>
                            
                            <div class="activity-item">
                                <div class="activity-icon info">
                                    <i class="fas fa-user-plus"></i>
                                </div>
                                <div class="activity-content">
                                    <div class="activity-title">Nuevo usuario registrado</div>
                                    <div class="activity-description">consultor@papaclick.com</div>
                                </div>
                                <div class="activity-time">Hace 3 horas</div>
                            </div>
                            
                            <div class="text-center mt-3">
                                <a href="/historial" class="btn btn-sm btn-outline-success">
                                    Ver todo el historial <i class="fas fa-arrow-right ms-1"></i>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- Performance Metrics Row -->
                <div class="row mt-4">
                    <div class="col-12">
                        <div class="chart-container animate__animated animate__fadeInUp animate__delay-3s">
                            <div class="chart-header">
                                <h4 class="mb-0">🎯 Métricas de Rendimiento del Sistema</h4>
                                <p class="text-muted mb-0">Estado actual del modelo de IA y procesamiento</p>
                            </div>
                            <div class="row mt-4">
                                <div class="col-md-3 text-center">
                                    <div class="d-inline-flex align-items-center justify-content-center bg-success bg-opacity-10 rounded-circle mb-3" style="width: 80px; height: 80px;">
                                        <i class="fas fa-tachometer-alt fs-2 text-success"></i>
                                    </div>
                                    <h5>Velocidad Promedio</h5>
                                    <p class="text-muted"><%= stats.tiempoPromedio %>s por imagen</p>
                                </div>
                                <div class="col-md-3 text-center">
                                    <div class="d-inline-flex align-items-center justify-content-center bg-info bg-opacity-10 rounded-circle mb-3" style="width: 80px; height: 80px;">
                                        <i class="fas fa-bullseye fs-2 text-info"></i>
                                    </div>
                                    <h5>Confianza Promedio</h5>
                                    <p class="text-muted"><%= stats.confianzaPromedio %>% de certeza</p>
                                </div>
                                <div class="col-md-3 text-center">
                                    <div class="d-inline-flex align-items-center justify-content-center bg-warning bg-opacity-10 rounded-circle mb-3" style="width: 80px; height: 80px;">
                                        <i class="fas fa-check-double fs-2 text-warning"></i>
                                    </div>
                                    <h5>Papas Aptas</h5>
                                    <p class="text-muted"><%= stats.aptos %> clasificadas</p>
                                </div>
                                <div class="col-md-3 text-center">
                                    <div class="d-inline-flex align-items-center justify-content-center bg-primary bg-opacity-10 rounded-circle mb-3" style="width: 80px; height: 80px;">
                                        <i class="fas fa-times-circle fs-2 text-primary"></i>
                                    </div>
                                    <h5>Papas No Aptas</h5>
                                    <p class="text-muted"><%= stats.noAptos %> clasificadas</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Chart.js for statistics -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    
    <!-- Custom Dashboard JavaScript -->
    <script>
        // Animate statistics on page load
        document.addEventListener('DOMContentLoaded', function() {
            // Animate stat values
            animateStatValues();
            
            // Load real-time data
            loadDashboardData();
            
            // Update data every 30 seconds
            setInterval(loadDashboardData, 30000);
            
            // Mobile sidebar toggle
            setupMobileSidebar();
        });
        
        function animateStatValues() {
            const statValues = document.querySelectorAll('.stat-value');
            statValues.forEach((stat, index) => {
                const finalValue = stat.textContent;
                stat.textContent = '0';
                
                setTimeout(() => {
                    const isPercentage = finalValue.includes('%');
                    const isTime = finalValue.includes('s');
                    let numericValue = parseFloat(finalValue.replace(/[^\d.]/g, ''));
                    
                    let currentValue = 0;
                    const increment = numericValue / 50; // 50 steps animation
                    
                    const counter = setInterval(() => {
                        currentValue += increment;
                        if (currentValue >= numericValue) {
                            currentValue = numericValue;
                            clearInterval(counter);
                        }
                        
                        let displayValue = Math.floor(currentValue);
                        if (isPercentage) {
                            displayValue = currentValue.toFixed(1) + '%';
                        } else if (isTime) {
                            displayValue = currentValue.toFixed(1) + 's';
                        } else if (numericValue > 1000) {
                            displayValue = Math.floor(currentValue).toLocaleString();
                        }
                        
                        stat.textContent = displayValue;
                    }, 20);
                }, index * 200);
            });
        }
        
        function loadDashboardData() {
            // Simulated data loading - replace with actual API calls
            console.log('Loading dashboard data...');
            
            // Update activity timestamps
            updateActivityTimestamps();
        }
        
        function updateActivityTimestamps() {
            const timeElements = document.querySelectorAll('.activity-time');
            timeElements.forEach(element => {
                const text = element.textContent;
                if (text.includes('min')) {
                    const minutes = parseInt(text.match(/\d+/)[0]);
                    element.textContent = `Hace ${minutes + 1} min`;
                }
            });
        }
        
        function setupMobileSidebar() {
            // Add mobile menu toggle if needed
            if (window.innerWidth < 992) {
                const sidebar = document.querySelector('.sidebar');
                let isMenuOpen = false;
                
                // Create toggle button
                const toggleBtn = document.createElement('button');
                toggleBtn.className = 'btn btn-success position-fixed';
                toggleBtn.style.cssText = 'top: 20px; left: 20px; z-index: 1100;';
                toggleBtn.innerHTML = '<i class="fas fa-bars"></i>';
                
                toggleBtn.addEventListener('click', () => {
                    if (isMenuOpen) {
                        sidebar.style.marginLeft = '-250px';
                        toggleBtn.innerHTML = '<i class="fas fa-bars"></i>';
                    } else {
                        sidebar.style.marginLeft = '0';
                        toggleBtn.innerHTML = '<i class="fas fa-times"></i>';
                    }
                    isMenuOpen = !isMenuOpen;
                });
                
                document.body.appendChild(toggleBtn);
            }
        }
        
        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
        
        // Tooltips initialization
        var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
        var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
            return new bootstrap.Tooltip(tooltipTriggerEl)
        });
        
        // Progress bars animation
        function animateProgressBars() {
            const progressBars = document.querySelectorAll('.progress-bar-eco');
            progressBars.forEach(bar => {
                const width = bar.style.width;
                bar.style.width = '0%';
                setTimeout(() => {
                    bar.style.width = width;
                }, 500);
            });
        }
        
        // Call progress bar animation after page load
        setTimeout(animateProgressBars, 1000);
        
        // Add hover effects to action buttons
        document.querySelectorAll('.action-btn').forEach(btn => {
            btn.addEventListener('mouseenter', function() {
                this.style.transform = 'translateY(-4px) scale(1.02)';
            });
            
            btn.addEventListener('mouseleave', function() {
                this.style.transform = 'translateY(0) scale(1)';
            });
        });
    </script>
</body>
</html>